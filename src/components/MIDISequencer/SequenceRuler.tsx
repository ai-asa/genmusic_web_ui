import React from 'react';
import { Box } from '@mui/material';

const SequenceRuler: React.FC = () => {
  return (
    <Box sx={{ height: 30, borderBottom: '1px solid #ccc' }}>
      {/* Implement ruler logic here */}
    </Box>
  );
};

export default SequenceRuler;